/// List of Enums
/// They cannot be created inside a class
library;


enum ProductType {single, variable}
enum TextSizes {small,medium,large}

enum OrderStatus {processing, ongoing, shipped, jobdone,  delivered, cancelled, scheduled, completed, rescheduled}


enum BookingStatus {processing,ongoing,scheduled, completed, rescheduled, cancelled}
enum PaymentMethods {paypal, googlepay, applepay, visa, masterCard, creditCard, paystack, razorPay, paytm, gcash, grabpay, maya}